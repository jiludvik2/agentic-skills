# State — last updated 2026-05-30

**Active focus:** `epic-analyzer-ga-hardening` (**8 stories, s0–s7 — all task-planned**) → **s0–s6 CLOSED**. **s6-install-skill-bundle CLOSED 2026-05-30** (s6-t0 ADR-0018, s6-t1 wheel packaging, s6-t2 install + CLI restructure; story-level Review Important remediated via fix1→fix2 chain). On branch `ccglass-traffic-analysis` — **12 commits ahead of origin, all UNPUSHED** (operator commits/pushes per AGENTS.md no-push policy). This session = **execution**: built + closed s6 (ADR + packaging + `polyreview install` + the `polyreview run` CLI restructure). **Only s7 (uninstall) remains in the epic.**
**Last completed:** **s6 story CLOSED.** `polyreview install` copies the wheel-shipped bundle into the agent-independent registry (`agents`/`claude`/`copilot`/`gemini`; neutral + auto-detect default), marker-guarded, idempotent/`--force`. New `code_review/bundle.py` (BUNDLE_MANIFEST SSOT) + `code_review/install.py`; pyproject `force-include` ships the bundle under `code_review/_bundle/`. **CLI restructured: review now runs as `polyreview run` (bare form dropped, ADR-0018 §3, operator-ratified)** — ~43 call sites + SKILL.md/README/AGENTS.md/release.yml/runbook all migrated (story-level Review caught the CI+runbook stragglers; fixed in s6-fix1/fix2). Suite **404 passed**; ruff + mypy clean.
**Next:** **s7-uninstall-skill-bundle** (last epic story). Plan **operator-APPROVED 2026-05-30**. Execution entry **s7-t0** = `polyreview uninstall` command, reusing `install.py`'s registry + env→home resolver + **frontmatter-anchored marker** (`is_our_bundle`, already built & tested for install's refuse/idempotency paths). Per ADR-0018 §5: remove only `<skills-dir>/code-review/`, marker-guarded, never touch siblings/`agents/reviewer.md`/homes, non-interactive, refusal reported (non-zero exit). **Halted at the s6 story boundary per the one-story-per-`/clear` cadence — resume with `/clear` → "resume s7".** After s7: bump `pyproject.toml` → `0.1.0`, then epic close (Document + File verbs). 12 commits unpushed (operator pushes).

## Open questions / follow-ups

- **GA supply-chain follow-up (open since s3 close):** project defines **no** dependency-audit gate (no `make audit`/Makefile, no pip-/npm-audit in CI), so SDLC rule #26 is skipped per its "no gate ⇒ skip" clause. `npm audit` on the vendored JS toolchain reports **5 pre-existing** transitive vulns (picomatch/micromatch ReDoS, smol-toml DoS) — wheel-excluded, offline, local-only. Decide before GA whether to wire an audit gate (ADR) and/or `npm audit fix`. Own task.
- **Diff-path resolution follow-up (open since s4 review):** `resolve_diff_paths` returns repo-relative paths and the orchestrator `abspath`'s them against `Path.cwd()` (cli.py:142, diff.py), so a `--diff` review launched from a subdir mis-resolves target paths — affects **all** adapters, pre-existing. Consider resolving diff paths against the repo root (or returning absolute paths). Own task.
- **GA release blocked on `epic-analyzer-ga-hardening`.** Analyzer set ✅ (s0–s5); install ✅ (s6). Remaining: **s7 uninstall**, then bump `pyproject.toml` → `0.1.0`, commit, cut + push GA tag `code-review-v0.1.0`. Runbook: `sdlc/docs/runbooks/release.md`.
- **s6 deferred Minors (own follow-ups):** (1) `polyreview install --force` is `rmtree`-then-copy — an I/O failure mid-copy leaves partial state; harden to copy-to-temp + atomic rename (+ an I/O-failure test). (2) On a mixed `--all` run with one refused target, the "Next: provision caches" hint prints before `Exit(1)`; suppress/reorder on refusal. (3) Bundle `code-review.toml.example` cross-references `python -m code_review.cli --help` (still exit 0; align to `run` when convenient).
- **Stale doc: `scripts/license_audit.py`** referenced by `stack-pins.md` (License floor §) but the file does not exist and no CI/Makefile step runs it. Add the gate (ADR + wiring) or correct stack-pins. Open since s1 close.
- **`claude-code-review` redirect meta-package** (ADR-0014): publish after the first GA publish.
- **`analyze_ccglass.py`** carries 22 ruff errors on this branch (pre-existing) — would redden CI if this branch merges. Out of epic scope.

## Recent shipped

- **(2026-05-30) s6-install-skill-bundle CLOSED** (`86badb3`) — install/uninstall packaging. `bundle.py` SSOT + `install.py` registry/resolver/marker + pyproject `force-include` (→ `code_review/_bundle/`) + `polyreview install` + CLI restructure to `polyreview run`. Story-level Review Important (CLI restructure not propagated to release pipeline) → fix1 (release.yml + guard test + AGENTS.md) → fix2 (release runbook), Review CLEAN. 404 passed. **Unpushed.**
- **(2026-05-30) s3/s4/s5 CLOSED** — F1 depcruiser (`ed0bec2`), F8 eslint cwd-anchor (`310fd51`), F10 CLI error branches (`3ab8280`). **Unpushed.**
- **s0/s1/s2 CLOSED** (F3 semgrep, F5+F9 JS toolchain/ADR-0017, F2 jscpd). **Pushed.**
- **(on `main`, e575b37)** `polyreview 0.1.0rc1` on TestPyPI via `release.yml`.
