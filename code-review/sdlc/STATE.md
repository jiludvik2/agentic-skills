# State — last updated 2026-05-30

**Active focus:** `epic-analyzer-ga-hardening` (**8 stories, s0–s7 — all task-planned**) → **s0 + s1 + s2 + s3 CLOSED**. **s3-depcruiser-node-compat CLOSED 2026-05-30** (two tasks; story-level Review MINOR-ONLY). On branch `ccglass-traffic-analysis` — **3 commits ahead of origin, UNPUSHED** (s3-t0 `6d0e3b0`, s3-t1 `6d75084`, s3 close `ed0bec2`). This session = **execution**: built + closed s3-depcruiser (F1). Remaining findings: F8 eslint config (s4), F10 CLI errors (s5). Sequencing = s4→s7.
**Last completed:** **s3 story CLOSED** (`ed0bec2`) — F1 fixed. The dependency-cruiser coupling analyzer now runs on modern Node and reports circular deps. s3-t0: pin `16.0.0→16.10.4` (empirical Node-fs/constants floor = **16.10.2**, not the "≈16.3" estimate — `16.10.1` imports `R_OK` from `node:fs`, `16.10.2` switched to `node:fs/constants`; verified on Node 24). s3-t1: adapter self-supplies a temp `cruise-config.cjs` via `--config` (`enhancedResolveOptions.extensions`, no `tsConfig`, no `forbidden`); **vendored `typescript@^5`** (knip's nested `@6` is out of depcruise's `>=2 <6` range, which disabled `.ts` enumeration for directory targets — the CLI default `.` scan). Integration test flipped `xfail(strict)`→real pass (`must_xfail` False); new `tests/fixtures/js-circular/`. Full suite **381 passed, 1 xfailed** (only eslint/s4 remains); ruff + mypy clean.
**Next:** **s4-eslint-adapter-robustness** (F8) — task `s4-t0-eslint-formatter-and-config-discovery`. Plan operator-approved. On close, flip `test_eslint_integration_detects_console_log` xfail→pass + its `must_xfail` False (same forcing function as jscpd/depcruiser — last remaining xfail). **Halted at the s3 story boundary per the one-story-per-`/clear` cadence — resume with `/clear` → "resume s4".** NOTE: 3 commits unpushed (no push policy in AGENTS.md → operator pushes); push `ccglass-traffic-analysis` when ready.

## Open questions / follow-ups

- **GA supply-chain follow-up (NEW 2026-05-30, surfaced at s3 close):** the project defines **no** dependency-audit gate (no `make audit`/Makefile, no pip-audit/npm-audit in CI), so SDLC rule #26 is skipped per its "no gate ⇒ skip" clause. `npm audit` on the vendored JS toolchain reports **5 pre-existing** transitive vulns (1 high picomatch ReDoS, micromatch ReDoS, smol-toml DoS) — present before s3, wheel-excluded, runs offline on local code. Decide before GA whether to wire an audit gate (ADR) and/or `npm audit fix`. Own task, out of s4/s5 scope.
- **s6/s7 install/uninstall plan — operator-APPROVED 2026-05-30** (agent-independent, target registry over `~/.agents`/`~/.claude`/`~/.copilot`/`~/.gemini`; default = neutral + auto-detected present homes; `--agent`/`--all` overrides; create dir if missing). Two ADR-0018 seams: wheel doesn't ship the bundle yet (s6-t1 fixes), `cache_root()` stays CWD-anchored. **APPROVED — cleared to execute; not yet started.** Sequencing = s4→s7. Execution entry: s6-t0 (ADR-0018) → s6-t1 → s6-t2 → s7-t0.
- **GA release blocked on `epic-analyzer-ga-hardening`.** ~~F3 semgrep~~ ✅ (s0); ~~F5 JS toolchain~~ ✅ + ~~F9 CI masks Node integration~~ ✅ (s1); ~~F2 jscpd~~ ✅ (s2); ~~F1 depcruiser/Node~~ ✅ (s3). Remaining blockers: **F8 eslint config (s4), F10 CLI errors (s5)**. Fix the rest, then bump `pyproject.toml` → `0.1.0`, commit, cut + push GA tag `code-review-v0.1.0`. Runbook: `sdlc/docs/runbooks/release.md`.
- **Stale doc: `scripts/license_audit.py`** referenced by `stack-pins.md` (License floor §) but the file does not exist and no CI/Makefile audit step runs it. Either add the gate (ADR + wiring) or correct stack-pins. Surfaced at s1 close; still open.
- **`claude-code-review` redirect meta-package** (ADR-0014): publish after the first GA publish.
- **`analyze_ccglass.py`** carries 22 ruff errors on this branch (pre-existing) — would redden CI if this branch merges. Out of epic scope.

## Recent shipped

- **(2026-05-30) s3-depcruiser-node-compat CLOSED** (`ed0bec2`) — F1. Pin 16.0.0→16.10.4 (floor 16.10.2); adapter supplies its own temp cruise config; vendored `typescript@^5` so depcruise enumerates `.ts` in directory targets. New `tests/fixtures/js-circular/`; integration test flipped xfail→real pass. Story-level Review MINOR-ONLY. **Unpushed.**
- **(2026-05-30) s2-jscpd-output-plumbing CLOSED** (`ecddd74`) — F2. jscpd writes to a `TemporaryDirectory` + parses `jscpd-report.json`. New `tests/fixtures/js-duplication/` byte-identical clone pair. **Pushed** (s1+s2 pushed at S504).
- **(2026-05-30) s1-js-toolchain-manifest CLOSED** — 4 tasks: ADR-0017 (Node 20+22, JS pins), vendored `package.json`/lockfile, drift guards, CI Node 20+22 integration job + xfail-gating. **Pushed.**
- **s0-semgrep-rule-source CLOSED** — semgrep green from a clean `setup.sh` (FINDINGS F3). **Pushed.**
- **Analyzer-coverage QA** — regenerable 13-adapter smoke test at `sdlc/docs/qa/analyzer-coverage/`.
- **(on `main`, e575b37)** `polyreview 0.1.0rc1` on TestPyPI via `release.yml`.
