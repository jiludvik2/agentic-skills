# State — last updated 2026-05-30

**Active focus:** `epic-analyzer-ga-hardening` (**8 stories, s0–s7 — all task-planned**) → **s0–s5 CLOSED**. **s5-cli-error-branch-coverage CLOSED 2026-05-30** (single task `s5-t0`; story-level Review = the per-task Review, MINOR-ONLY). On branch `ccglass-traffic-analysis` — **6 commits ahead of origin, all UNPUSHED** (3× s3 + wrap `97938e6`, then s4 `310fd51`, s5 `3ab8280`; operator commits/pushes per AGENTS.md no-push policy). This session = **execution**: committed the prior session's closed-but-uncommitted s4 work (rule #20), then built + closed s5 (F10). **All analyzer-set GA blockers (s0–s5) now cleared.** Remaining epic work: s6/s7 install/uninstall.
**Last completed:** **s5 story CLOSED** — F10. `tests/test_cli_error_branches.py` locks the three previously-uncovered `cli.py` error branches — unknown `--analyzer`, explicitly-selected disabled analyzer, empty post-filter selection — each asserting non-zero exit **and** the specific stderr contract. Test-only; no production change (branches already behaved → green on first run). Empty-selection driven end-to-end via `--review security --language go` (all security analyzers are python/js/ts-only → filter empties the set with `error=None` → reaches cli.py:315), pinned by a positive control against capabilities.json drift. Verify PASS; Review MINOR-ONLY (1 Minor hardened in-task, 1 deferred to task notes). Suite **387 passed**; ruff + mypy clean.
**Next:** **s6 install/uninstall** (epic-deployment-readiness work). Plan **operator-APPROVED 2026-05-30** (agent-independent, registry over `~/.agents`/`~/.claude`/`~/.copilot`/`~/.gemini`). Execution entry: **s6-t0 = ADR-0018** (wheel-doesn't-ship-bundle + `cache_root()` stays CWD-anchored seams) → s6-t1 → s6-t2 → s7-t0. **Halted at the s5 story boundary per the one-story-per-`/clear` cadence — resume with `/clear` → "resume s6".** NOTE: ADR-0018 content needs operator sign-off (ADRs stay human). 6 commits unpushed (operator pushes).

## Open questions / follow-ups

- **GA supply-chain follow-up (open since s3 close):** project defines **no** dependency-audit gate (no `make audit`/Makefile, no pip-/npm-audit in CI), so SDLC rule #26 is skipped per its "no gate ⇒ skip" clause. `npm audit` on the vendored JS toolchain reports **5 pre-existing** transitive vulns (picomatch/micromatch ReDoS, smol-toml DoS) — wheel-excluded, offline, local-only. Decide before GA whether to wire an audit gate (ADR) and/or `npm audit fix`. Own task.
- **Diff-path resolution follow-up (open since s4 review):** `resolve_diff_paths` returns repo-relative paths and the orchestrator `abspath`'s them against `Path.cwd()` (cli.py:142, diff.py), so a `--diff` review launched from a subdir mis-resolves target paths — affects **all** adapters, pre-existing. Consider resolving diff paths against the repo root (or returning absolute paths). Own task.
- **GA release blocked on `epic-analyzer-ga-hardening`.** Analyzer set ✅ (s0 semgrep/F3, s1 JS toolchain/F5+F9, s2 jscpd/F2, s3 depcruiser/F1, s4 eslint/F8, s5 CLI errors/F10). Remaining: **s6/s7 install/uninstall**, then bump `pyproject.toml` → `0.1.0`, commit, cut + push GA tag `code-review-v0.1.0`. Runbook: `sdlc/docs/runbooks/release.md`.
- **Stale doc: `scripts/license_audit.py`** referenced by `stack-pins.md` (License floor §) but the file does not exist and no CI/Makefile step runs it. Add the gate (ADR + wiring) or correct stack-pins. Open since s1 close.
- **`claude-code-review` redirect meta-package** (ADR-0014): publish after the first GA publish.
- **`analyze_ccglass.py`** carries 22 ruff errors on this branch (pre-existing) — would redden CI if this branch merges. Out of epic scope.

## Recent shipped

- **(2026-05-30) s5-cli-error-branch-coverage CLOSED** (`3ab8280`) — F10. `tests/test_cli_error_branches.py` locks the three `cli.py` error branches (unknown `--analyzer`, disabled analyzer, empty selection); test-only, green on first run; +positive control vs capabilities.json drift. Review MINOR-ONLY. **Unpushed.**
- **(2026-05-30) s4-eslint-adapter-robustness CLOSED** (`310fd51`) — F8. Adapter anchors eslint's subprocess cwd at the targets' common-ancestor (child-only, preserves `cache_root()`) so eslint v9 finds the reviewed project's flat config; new `tests/fixtures/js-eslint/`; integration test de-xfailed (suite 0 xfailed). Committed this session. **Unpushed.**
- **(2026-05-30) s3-depcruiser-node-compat CLOSED** (`ed0bec2`) — F1. Pin 16.10.4; adapter supplies own temp cruise config; vendored `typescript@^5`. **Unpushed.**
- **(2026-05-30) s2-jscpd-output-plumbing CLOSED** (`ecddd74`) — F2. jscpd → `TemporaryDirectory` + parse `jscpd-report.json`. **Pushed.**
- **s1-js-toolchain-manifest CLOSED** — ADR-0017, vendored manifest/lockfile, drift guards, CI Node 20+22 job. **Pushed.**
- **s0-semgrep-rule-source CLOSED** — F3. **Pushed.**
- **(on `main`, e575b37)** `polyreview 0.1.0rc1` on TestPyPI via `release.yml`.
