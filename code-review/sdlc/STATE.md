# State — last updated 2026-05-30

**Active focus:** `epic-analyzer-ga-hardening` (**8 stories, s0–s7 — all now task-planned**) → **s0 CLOSED**; **s1-js-toolchain-manifest in progress** (4 tasks; s1-t0 + s1-t1 done, **s1-t2 next**). On branch `ccglass-traffic-analysis` (pushed, even with origin @ `9aa34ec`). Remaining findings: F8 eslint (s4), F9 CI masks skipped Node integration tests (s1-t3 + s2/s3/s4), F10 CLI error branches (s5). This session = **planning only, no execution**: drafted task plans for s2/s3/s4/s5, made s6/s7 install/uninstall **agent-independent**, retired orphan `s3-plan.md`/`s4-plan.md` to done/. **s6/s7 plans await operator approval** (default-target policy confirmed); s2–s5 plans ready. See Open questions.
**Last completed:** **s1-t1** (`f397057`) — committed `package.json` + `package-lock.json` at the skill root pinning eslint^9/knip^5/jscpd^4/dependency-cruiser^16(candidate)/eslint-formatter-sarif^3 (ADR-0017); setup.sh's npm-ci branch now vendors them; 4 Node analyzers probe available. s1-t0 (`dbe3a4e`) = ADR-0017 (Node 20+22 matrix) + stack-pins. Verify PASS / Review MINOR-ONLY both. (Earlier: whole s0 story closed `c19b786`.)
**Next:** **s1-t2** — version-drift guard (capabilities.json has NO Node-tool version fields today → add a version source that matches the lockfile) + make the eslint SARIF formatter (`@microsoft/eslint-formatter-sarif`) resolve regardless of adapter cwd (replace the smoke harness's NODE_PATH stopgap). Spec: `sdlc/work/active/s1-t2-version-drift-and-eslint-formatter.md`. Then s1-t3 (CI Node 20+22 matrix runs integration tests, xfail-gating jscpd/depcruiser/eslint → flipped in s2/s3/s4). **Paused at clean task boundary for context — resume with `/clear` → "resume s1-t2".**

## Open questions / follow-ups

- **All GA-epic stories now task-planned** (2026-05-30): s2/s3/s4/s5 planned this session — `s2-t0-jscpd-tempdir-report`; `s3-t0-pin-depcruiser-node-compatible` + `s3-t1-cruise-config-and-circular-detection` (depend on s1 closed); `s4-t0-eslint-formatter-and-config-discovery`; `s5-t0-cli-error-branch-tests`. s4/s5 stories trimmed their inline test spec to a one-line task pointer (single source of truth in the task file). Every s0–s7 story now has a plan (s0 done; s1/s6/s7 already planned). Orphan plan docs `s3-plan.md`/`s4-plan.md` (parents already in done/) moved to `work/done/` and marked `status: done` — `active/` no longer carries stale plans.
- **NEW — s6/s7 install/uninstall plan awaiting approval** (2026-05-30, **revised agent-independent**): drafted `s6-install-skill-bundle` (t0 ADR-0018, t1 wheel-packages the bundle, t2 `polyreview install`) + `s7-uninstall-skill-bundle` (t0 `polyreview uninstall`); epic children + scope note updated. **Build + test** real commands, **user-level** target, housed under this epic (operator decisions). **Agent-independent (operator directive 2026-05-30):** not Claude-only — a target **registry** resolves the right user-level skills dir per agent: neutral `~/.agents/skills/` (read by Codex, aliased by Gemini), `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/`, `~/.copilot/skills/`, `~/.gemini/skills/`; default = neutral **plus auto-detected present homes**, `--agent`/`--all` overrides, **create dir if missing** (covers "`.claude` doesn't exist yet"). Two ADR-0018 seams: the wheel does **not** currently ship the bundle (only `code_review/` + JSON; s6-t1 fixes), and `cache_root()` stays CWD-anchored (caches via `setup.sh`/`POLYREVIEW_CACHE_DIR`, not relocated). **Default-target policy (neutral+auto-detect vs `--all`) is the one open call for operator sign-off.** Story IDs renamed from `*-into/from-claude` (pre-approval correction). **Plan approval is human — execution not started.** Independent of s1–s5 (current decision: finish s1 first).
- **RESOLVED — ADR-0016 JS/TS rule scope** (2026-05-30): operator chose to **amend** ADR-0016 #2 to "Python-first; JS/TS a tracked follow-up" (not author JS/TS rules now). s0 stays closed on the proven Python coverage. Amendment note in `adr-0016-semgrep-rule-provenance.md` #2.
- **GA release blocked on `epic-analyzer-ga-hardening`.** ~~F3 semgrep~~ ✅ resolved (s0). Remaining blockers: F1 depcruiser/Node, F2 jscpd, F5 JS toolchain (+ F8 eslint, F10 CLI errors). Fix the rest, then bump `pyproject.toml` → `0.1.0`, commit, cut + push GA tag `code-review-v0.1.0`. Runbook: `sdlc/docs/runbooks/release.md`.
- **`claude-code-review` redirect meta-package** (ADR-0014): publish after the first GA publish.
- **`analyze_ccglass.py`** carries 22 ruff errors on this branch (pre-existing, prior session) — would redden CI if this branch merges. Out of epic scope.

## Recent shipped (2026-05-29)

- **s1 in progress** — s1-t0 ADR-0017 (Node 20+22 matrix, JS pins) + s1-t1 vendored `package.json`/lockfile (4 Node analyzers now `available`). `dbe3a4e`/`f397057`, pushed.
- **s0-semgrep-rule-source CLOSED** — semgrep green from a clean `setup.sh` (vendored ruleset + cache_root + fail-loud + e2e). FINDINGS F3 resolved. Through `c19b786`, pushed.
- **Analyzer-coverage QA** (committed earlier this branch): regenerable smoke test (`scaffold_fixtures.sh` + `run_smoke.py`) covering all 13 adapters; the smoke harness now asserts semgrep rules are provisioned (no longer self-provisions).
- **(on `main`, e575b37) CI green**, test suite strict-typed; `polyreview 0.1.0rc1` on TestPyPI via `release.yml`.
