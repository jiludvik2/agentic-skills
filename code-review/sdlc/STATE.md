# State — last updated 2026-05-30

**Active focus:** `epic-analyzer-ga-hardening` (**8 stories, s0–s7 — all task-planned**) → **s0 + s1 CLOSED**. **s1-js-toolchain-manifest CLOSED 2026-05-30** (all 4 tasks done; story-level Review MINOR-ONLY). On branch `ccglass-traffic-analysis` — **3 commits ahead of origin, UNPUSHED** (`a9cae11` s1-t2, `0edc70a` s1-t3, `9ad7ed2` s1 close). This session = **execution**: built s1-t2 + s1-t3, closed story s1. **Next story = s2-jscpd (F2), plan ready/approved.** Remaining findings post-s1: F1 depcruiser/Node (s3), F2 jscpd (s2), F8 eslint config (s4), F10 CLI errors (s5). Sequencing = s2→s7.
**Last completed:** **s1 story CLOSED** (`9ad7ed2`) — JS toolchain pinned, vendored, and CI-exercised. **s1-t3** (`0edc70a`): new `ci.yml` `node-integration` job (Node 20+22 matrix) `npm ci`s the toolchain + runs the Node integration tests (F9 fixed); jscpd/depcruiser/eslint `xfail(strict)` → s2/s3/s4; meta-test enforces skip→xfail. **s1-t2** (`a9cae11`): capabilities version-drift guard vs lockfile (incl. the SARIF formatter); eslint adapter exports NODE_PATH so the formatter resolves from any cwd. Verify PASS + Review MINOR-ONLY for both; story-level Review MINOR-ONLY. Full suite **375 passed, 3 xfailed**; ruff + mypy strict clean.
**Next:** **s2-jscpd** (F2) — first task `s2-t0-jscpd-tempdir-report`. On close, **flip `test_jscpd_integration` from xfail(strict) → real pass** (the xfail will XPASS-fail until then, which is the forcing function). Plan is operator-approved. **Halted at the s1 story boundary for context — resume with `/clear` → "resume s2".** NOTE: 3 commits are unpushed (no push policy in AGENTS.md → operator pushes); push `ccglass-traffic-analysis` when ready.

## Open questions / follow-ups

- **All GA-epic stories now task-planned** (2026-05-30): s2/s3/s4/s5 planned this session — `s2-t0-jscpd-tempdir-report`; `s3-t0-pin-depcruiser-node-compatible` + `s3-t1-cruise-config-and-circular-detection` (depend on s1 closed); `s4-t0-eslint-formatter-and-config-discovery`; `s5-t0-cli-error-branch-tests`. s4/s5 stories trimmed their inline test spec to a one-line task pointer (single source of truth in the task file). Every s0–s7 story now has a plan (s0 done; s1/s6/s7 already planned). Orphan plan docs `s3-plan.md`/`s4-plan.md` (parents already in done/) moved to `work/done/` and marked `status: done` — `active/` no longer carries stale plans.
- **s6/s7 install/uninstall plan — operator-APPROVED 2026-05-30** (revised agent-independent): drafted `s6-install-skill-bundle` (t0 ADR-0018, t1 wheel-packages the bundle, t2 `polyreview install`) + `s7-uninstall-skill-bundle` (t0 `polyreview uninstall`); epic children + scope note updated. **Build + test** real commands, **user-level** target, housed under this epic (operator decisions). **Agent-independent (operator directive 2026-05-30):** not Claude-only — a target **registry** resolves the right user-level skills dir per agent: neutral `~/.agents/skills/` (read by Codex, aliased by Gemini), `${CLAUDE_CONFIG_DIR:-~/.claude}/skills/`, `~/.copilot/skills/`, `~/.gemini/skills/`; default = neutral **plus auto-detected present homes**, `--agent`/`--all` overrides, **create dir if missing** (covers "`.claude` doesn't exist yet"). Two ADR-0018 seams: the wheel does **not** currently ship the bundle (only `code_review/` + JSON; s6-t1 fixes), and `cache_root()` stays CWD-anchored (caches via `setup.sh`/`POLYREVIEW_CACHE_DIR`, not relocated). Default-target policy = **neutral + auto-detect** (operator-confirmed). Story IDs renamed from `*-into/from-claude` (pre-approval correction). **APPROVED — cleared to execute; not yet started.** Independent of s1–s5; sequencing = finish s1 first, then s2→s7. Execution entry: s6-t0 (ADR-0018) → s6-t1 → s6-t2 → s7-t0.
- **RESOLVED — ADR-0016 JS/TS rule scope** (2026-05-30): operator chose to **amend** ADR-0016 #2 to "Python-first; JS/TS a tracked follow-up" (not author JS/TS rules now). s0 stays closed on the proven Python coverage. Amendment note in `adr-0016-semgrep-rule-provenance.md` #2.
- **GA release blocked on `epic-analyzer-ga-hardening`.** ~~F3 semgrep~~ ✅ (s0); ~~F5 JS toolchain~~ ✅ + ~~F9 CI masks Node integration~~ ✅ (s1). Remaining blockers: F2 jscpd (s2), F1 depcruiser/Node (s3), F8 eslint config (s4), F10 CLI errors (s5). Fix the rest, then bump `pyproject.toml` → `0.1.0`, commit, cut + push GA tag `code-review-v0.1.0`. Runbook: `sdlc/docs/runbooks/release.md`.
- **Stale doc: `scripts/license_audit.py`** referenced by `stack-pins.md` (License floor §) and described as CI-enforced, but the file does not exist and no CI/Makefile audit step runs it. Either add the gate (ADR + rule #26 wiring) or correct stack-pins. Surfaced at s1 close; out of s1 scope.
- **`claude-code-review` redirect meta-package** (ADR-0014): publish after the first GA publish.
- **`analyze_ccglass.py`** carries 22 ruff errors on this branch (pre-existing, prior session) — would redden CI if this branch merges. Out of epic scope.

## Recent shipped

- **(2026-05-30) s1-js-toolchain-manifest CLOSED** — all 4 tasks. s1-t2 drift-guard + eslint NODE_PATH (`a9cae11`); s1-t3 CI Node 20+22 integration job + xfail-gating (`0edc70a`); story close (`9ad7ed2`). Story-level Review MINOR-ONLY. **Unpushed.**
- **(2026-05-29) s1 t0/t1** — s1-t0 ADR-0017 (Node 20+22 matrix, JS pins) + s1-t1 vendored `package.json`/lockfile (4 Node analyzers now `available`). `dbe3a4e`/`f397057`, pushed.
- **s0-semgrep-rule-source CLOSED** — semgrep green from a clean `setup.sh` (vendored ruleset + cache_root + fail-loud + e2e). FINDINGS F3 resolved. Through `c19b786`, pushed.
- **Analyzer-coverage QA** (committed earlier this branch): regenerable smoke test (`scaffold_fixtures.sh` + `run_smoke.py`) covering all 13 adapters; the smoke harness now asserts semgrep rules are provisioned (no longer self-provisions).
- **(on `main`, e575b37) CI green**, test suite strict-typed; `polyreview 0.1.0rc1` on TestPyPI via `release.yml`.
