# claude-code-review

AI-native code review skill — a deterministic analyzer layer that runs Semgrep, Radon, Bandit, and more against a diff or target tree, returning consolidated SARIF + metrics JSON.

See [SKILL.md](.claude/skills/code-review/SKILL.md) for usage details.

> Full README content is drafted in story `s1-package-publication` task `s1-t1`. This file currently exists as a stub so PyPI metadata (`pyproject.toml`'s `readme = "README.md"`) resolves and `uv build` produces a wheel.
