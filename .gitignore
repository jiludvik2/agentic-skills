# macOS
.DS_Store

# editor
*.swp
*~
.vscode/
.idea/

# python
__pycache__/
*.pyc
.pytest_cache/

# local virtualenvs / node
.venv/
venv/
node_modules/

# Claude Code local settings (machine-specific personal overrides)
.claude/settings.local.json

# session artefacts (do not commit local Claude session paths)
local_*/
sessions/

# ad-hoc ccglass traffic/token analysis (local-only; not part of the code-review
# project — kept on disk, never tracked: report output + its generator script)
*ccglass*
.cache/
